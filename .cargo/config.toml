# `cargo xtask <task>` alias for `cargo run -p xtask -- <task>`.
[alias]
xtask = "run --package xtask --"

# Resolve dependencies to versions compatible with `rust-version` (MSRV), so
# `cargo update` and new lock entries never require a newer compiler.
[resolver]
incompatible-rust-versions = "fallback"
